<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allow sharing files from cache directory -->
    <cache-path
        name="cache"
        path="." />

    <!-- Optional: Allow sharing from external storage if needed -->
    <external-path
        name="external"
        path="." />
</paths>

